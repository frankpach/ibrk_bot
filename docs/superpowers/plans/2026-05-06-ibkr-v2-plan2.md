# IBKR AI Trader v2 — Plan 2: Dashboard + Reporte + Alertas + Estrategias

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Agregar dashboard web accesible desde el telefono, reporte semanal automatico por Telegram, alertas de precio configurables via bot, y prompts especializados por categoria de simbolo.

**Architecture:** Dashboard es un endpoint FastAPI que sirve HTML puro generado en Python — sin frameworks frontend. Alertas se almacenan en SQLite y se verifican cada 2 minutos junto con el position manager. Reporte semanal es un job APScheduler que corre cada lunes 8am ET. Estrategias por simbolo agregan una categoria a symbol_config y el agente elige el prompt segun la categoria.

**Tech Stack:** Python 3.13, FastAPI, SQLite, APScheduler, feedparser (ya instalado), python-telegram-bot (ya instalado)

**Flujo de trabajo:** Escribir archivo local → pytest local → scp a Pi → verificar integracion

---

## Mapa de Archivos

```
~/ibkr-bot/
├── app/
│   ├── api/
│   │   ├── main.py              MODIFICAR — agregar GET /dashboard
│   │   └── dashboard.py         CREAR — genera HTML del dashboard
│   ├── alerts/
│   │   ├── __init__.py          CREAR
│   │   └── manager.py           CREAR — CRUD alertas + verificacion
│   ├── reports/
│   │   ├── __init__.py          CREAR
│   │   └── weekly.py            CREAR — genera y envia reporte semanal
│   └── llm/
│       └── agent.py             MODIFICAR — prompts por categoria de simbolo
├── run.py                       MODIFICAR — agregar jobs de alertas y reporte semanal
└── tests/
    ├── test_dashboard.py         CREAR
    ├── test_alerts.py            CREAR
    ├── test_weekly_report.py     CREAR
    └── test_agent_strategies.py  CREAR
```

---

## Task 1: Alertas de precio configurables

**Files:**
- Create: `app/alerts/__init__.py`
- Create: `app/alerts/manager.py`
- Modify: `app/db/database.py` — tabla alerts + CRUD
- Modify: `app/notifications/telegram_bot.py` — comando /alerta
- Create: `tests/test_alerts.py`

**Estrategia:** Tabla `alerts` en SQLite guarda (symbol, threshold_pct, direction, created_at, triggered_at). Cada 2 minutos junto al position manager se verifica si el precio se movio mas del umbral vs el cierre del dia anterior. Si se dispara, notifica por Telegram y marca como triggered.

- [ ] **Step 1: Escribir tests de alertas LOCALMENTE**

Crear `C:\Users\be47\AppData\Local\Temp\test_alerts.py`:

```python
# tests/test_alerts.py
from unittest.mock import patch, MagicMock
from app.alerts.manager import (
    parse_alert_command,
    check_alert_triggered,
    AlertConfig,
)


def test_parse_alert_command_basic():
    alert = parse_alert_command("TSLA", "5%")
    assert alert.symbol == "TSLA"
    assert alert.threshold_pct == 0.05


def test_parse_alert_command_decimal():
    alert = parse_alert_command("AAPL", "2.5%")
    assert alert.symbol == "AAPL"
    assert alert.threshold_pct == 0.025


def test_parse_alert_command_invalid_returns_none():
    alert = parse_alert_command("AAPL", "abc")
    assert alert is None


def test_check_alert_triggered_on_rise():
    alert = AlertConfig(id=1, symbol="TSLA", threshold_pct=0.05)
    triggered, pct = check_alert_triggered(alert, current_price=210.0, prev_close=200.0)
    assert triggered is True
    assert pct == pytest.approx(0.05)


def test_check_alert_triggered_on_drop():
    alert = AlertConfig(id=1, symbol="TSLA", threshold_pct=0.05)
    triggered, pct = check_alert_triggered(alert, current_price=190.0, prev_close=200.0)
    assert triggered is True
    assert pct == pytest.approx(-0.05)


def test_check_alert_not_triggered_small_move():
    alert = AlertConfig(id=1, symbol="TSLA", threshold_pct=0.05)
    triggered, pct = check_alert_triggered(alert, current_price=203.0, prev_close=200.0)
    assert triggered is False


import pytest
```

- [ ] **Step 2: Correr tests localmente — verificar que fallan**

```bash
cd C:\Users\be47\AppData\Local\Temp && python -m pytest test_alerts.py -v 2>&1 | tail -5
```
Expected: ModuleNotFoundError (no tenemos el modulo local — OK, solo verificamos la logica)

- [ ] **Step 3: Crear app/alerts/__init__.py y app/alerts/manager.py LOCALMENTE**

Crear `C:\Users\be47\AppData\Local\Temp\alerts_manager.py`:

```python
# app/alerts/manager.py
"""
Gestiona alertas de precio configuradas por el usuario via Telegram.
Verifica cada 2 minutos si el precio se movio mas del umbral.
"""
import logging
from dataclasses import dataclass
from datetime import datetime
from typing import Optional

import httpx

from app.notifications.telegram import notify

logger = logging.getLogger(__name__)

API_BASE = "http://127.0.0.1:8088"


@dataclass
class AlertConfig:
    id: Optional[int]
    symbol: str
    threshold_pct: float  # ej: 0.05 = 5%


def parse_alert_command(symbol: str, threshold_str: str) -> Optional[AlertConfig]:
    """
    Parsea el comando /alerta TSLA 5% y retorna un AlertConfig.
    Retorna None si el formato es invalido.
    """
    try:
        pct_str = threshold_str.strip().rstrip("%")
        pct = float(pct_str) / 100.0
        if pct <= 0 or pct > 1.0:
            return None
        return AlertConfig(id=None, symbol=symbol.upper(), threshold_pct=pct)
    except (ValueError, AttributeError):
        return None


def check_alert_triggered(
    alert: AlertConfig,
    current_price: float,
    prev_close: float,
) -> tuple[bool, float]:
    """
    Verifica si el precio se movio mas del umbral vs el cierre anterior.
    Retorna (triggered, pct_change).
    """
    if prev_close <= 0:
        return False, 0.0
    pct_change = (current_price - prev_close) / prev_close
    triggered = abs(pct_change) >= alert.threshold_pct
    return triggered, round(pct_change, 4)


def _get_price_and_prev_close(symbol: str) -> tuple[float, float]:
    """Obtiene precio actual y cierre anterior via FastAPI."""
    try:
        data = httpx.get(f"{API_BASE}/price/{symbol}", timeout=15).json()
        current = data.get("market_price", 0.0)
        prev_close = data.get("close", current)  # close = delayed close price
        return current, prev_close
    except Exception as e:
        logger.error(f"Could not fetch price for {symbol}: {e}")
        return 0.0, 0.0


def check_all_alerts(db_get_alerts, db_mark_triggered):
    """
    Verifica todas las alertas activas y notifica las que se dispararon.
    db_get_alerts: callable -> list[AlertConfig]
    db_mark_triggered: callable(alert_id) -> None
    """
    alerts = db_get_alerts()
    if not alerts:
        return

    for alert in alerts:
        current_price, prev_close = _get_price_and_prev_close(alert.symbol)
        if current_price <= 0:
            continue

        triggered, pct_change = check_alert_triggered(alert, current_price, prev_close)

        if triggered:
            direction = "subio" if pct_change > 0 else "bajo"
            notify(
                f"ALERTA: <b>{alert.symbol}</b> {direction} {abs(pct_change):.1%}\n"
                f"Precio: ${current_price:.2f}\n"
                f"Umbral configurado: {alert.threshold_pct:.0%}"
            )
            db_mark_triggered(alert.id)
            logger.info(f"Alert triggered for {alert.symbol}: {pct_change:.1%}")
```

- [ ] **Step 4: Agregar tabla alerts y funciones CRUD a database.py en la Pi**

```bash
ssh aiutox-pi "tail -5 ~/ibkr-bot/app/db/database.py"
```

Leer database.py y agregar al final:

```python
# --- Alerts CRUD ---

def init_alerts_table():
    """Crea la tabla alerts si no existe. Llamar desde init_db()."""
    conn = get_connection()
    conn.execute("""
        CREATE TABLE IF NOT EXISTS alerts (
            id INTEGER PRIMARY KEY AUTOINCREMENT,
            symbol TEXT NOT NULL,
            threshold_pct REAL NOT NULL,
            created_at TEXT NOT NULL,
            triggered_at TEXT
        )
    """)
    conn.commit()
    conn.close()


def insert_alert(symbol: str, threshold_pct: float) -> int:
    conn = get_connection()
    cur = conn.execute(
        "INSERT INTO alerts (symbol, threshold_pct, created_at) VALUES (?,?,?)",
        (symbol.upper(), threshold_pct, datetime.utcnow().isoformat())
    )
    conn.commit()
    row_id = cur.lastrowid
    conn.close()
    return row_id


def get_active_alerts() -> list:
    from app.alerts.manager import AlertConfig
    conn = get_connection()
    rows = conn.execute(
        "SELECT * FROM alerts WHERE triggered_at IS NULL ORDER BY created_at ASC"
    ).fetchall()
    conn.close()
    return [AlertConfig(id=r["id"], symbol=r["symbol"], threshold_pct=r["threshold_pct"]) for r in rows]


def mark_alert_triggered(alert_id: int):
    conn = get_connection()
    conn.execute(
        "UPDATE alerts SET triggered_at=? WHERE id=?",
        (datetime.utcnow().isoformat(), alert_id)
    )
    conn.commit()
    conn.close()


def delete_alert(alert_id: int):
    conn = get_connection()
    conn.execute("DELETE FROM alerts WHERE id=?", (alert_id,))
    conn.commit()
    conn.close()


def get_all_alerts() -> list:
    from app.alerts.manager import AlertConfig
    conn = get_connection()
    rows = conn.execute("SELECT * FROM alerts ORDER BY created_at DESC").fetchall()
    conn.close()
    return [AlertConfig(id=r["id"], symbol=r["symbol"], threshold_pct=r["threshold_pct"]) for r in rows]
```

Y agregar `init_alerts_table()` dentro de `init_db()` al final del bloque executescript.

- [ ] **Step 5: SCP archivos a la Pi y ejecutar tests**

```bash
# SCP manager
scp C:\Users\be47\AppData\Local\Temp\alerts_manager.py aiutox-pi:~/ibkr-bot/app/alerts/manager.py
scp C:\Users\be47\AppData\Local\Temp\test_alerts.py aiutox-pi:~/ibkr-bot/tests/test_alerts.py

# Crear __init__.py
ssh aiutox-pi "mkdir -p ~/ibkr-bot/app/alerts && touch ~/ibkr-bot/app/alerts/__init__.py"

# Ejecutar tests en Pi
ssh aiutox-pi "cd ~/ibkr-bot && source .venv/bin/activate && pytest tests/test_alerts.py -v 2>&1"
```
Expected: 6 PASS

- [ ] **Step 6: Agregar /alerta al telegram_bot.py**

Leer telegram_bot.py y agregar despues de cmd_aprobar:

```python
@_only_owner
async def cmd_alerta(update: Update, ctx: ContextTypes.DEFAULT_TYPE):
    """
    /alerta SYMBOL THRESHOLD
    Ejemplo: /alerta TSLA 5%
    Notifica si TSLA sube o baja mas de 5% en el dia.
    """
    if len(ctx.args) < 2:
        await update.message.reply_text(
            "Uso: /alerta SYMBOL UMBRAL\n"
            "Ejemplo: /alerta TSLA 5%\n\n"
            "Para ver alertas activas: /alertas\n"
            "Para eliminar: /eliminar_alerta ID"
        )
        return
    symbol = ctx.args[0].upper()
    threshold_str = ctx.args[1]

    from app.alerts.manager import parse_alert_command
    alert = parse_alert_command(symbol, threshold_str)
    if alert is None:
        await update.message.reply_text(
            f"Formato invalido. Usa: /alerta {symbol} 5%"
        )
        return

    from app.db.database import insert_alert
    alert_id = insert_alert(alert.symbol, alert.threshold_pct)
    await update.message.reply_text(
        f"Alerta creada (ID:{alert_id})\n"
        f"{symbol} notificara si sube o baja mas del {alert.threshold_pct:.0%}"
    )


@_only_owner
async def cmd_alertas(update: Update, ctx: ContextTypes.DEFAULT_TYPE):
    """Lista todas las alertas activas."""
    from app.db.database import get_active_alerts
    alerts = get_active_alerts()
    if not alerts:
        await update.message.reply_text("Sin alertas activas.")
        return
    lines = [f"ID:{a.id} {a.symbol} umbral:{a.threshold_pct:.0%}" for a in alerts]
    await update.message.reply_text("Alertas activas:\n" + "\n".join(lines))


@_only_owner
async def cmd_eliminar_alerta(update: Update, ctx: ContextTypes.DEFAULT_TYPE):
    """Elimina una alerta por ID."""
    if not ctx.args:
        await update.message.reply_text("Uso: /eliminar_alerta ID")
        return
    try:
        alert_id = int(ctx.args[0])
        from app.db.database import delete_alert
        delete_alert(alert_id)
        await update.message.reply_text(f"Alerta {alert_id} eliminada.")
    except ValueError:
        await update.message.reply_text("ID debe ser un numero.")
```

Y registrar los handlers en `start_bot()`:
```python
app.add_handler(CommandHandler("alerta", cmd_alerta))
app.add_handler(CommandHandler("alertas", cmd_alertas))
app.add_handler(CommandHandler("eliminar_alerta", cmd_eliminar_alerta))
```

Y actualizar /ayuda para incluir:
```
  /alerta SYMBOL UMBRAL - crear alerta de precio
  /alertas - ver alertas activas
  /eliminar_alerta ID - eliminar alerta
```

- [ ] **Step 7: Agregar check_all_alerts al run.py**

En run.py, agregar job al scheduler:
```python
from app.alerts.manager import check_all_alerts
from app.db.database import get_active_alerts, mark_alert_triggered

scheduler.add_job(
    lambda: check_all_alerts(get_active_alerts, mark_alert_triggered),
    "interval",
    minutes=POSITION_CHECK_MINUTES,
    id="alert_checker",
)
```

- [ ] **Step 8: Reiniciar y probar**

```bash
ssh aiutox-pi "kill \$(ps aux | grep 'run.py' | grep -v grep | awk '{print \$2}') 2>/dev/null; sleep 2"
ssh aiutox-pi "cd ~/ibkr-bot && source .venv/bin/activate && nohup python3 run.py > /tmp/run.log 2>&1 & sleep 10 && curl -s http://127.0.0.1:8088/health"
```

Enviar `/alerta AAPL 1%` desde Telegram y verificar que responde.

- [ ] **Step 9: Commit**

```bash
ssh aiutox-pi "cd ~/ibkr-bot && git add app/alerts/ app/db/database.py app/notifications/telegram_bot.py run.py tests/test_alerts.py && git commit -m 'feat: price alerts via Telegram /alerta command with SQLite persistence'"
```

---

## Task 2: Reporte semanal automatico

**Files:**
- Create: `app/reports/__init__.py`
- Create: `app/reports/weekly.py`
- Modify: `run.py` — agregar job lunes 8am ET
- Create: `tests/test_weekly_report.py`

- [ ] **Step 1: Escribir tests LOCALMENTE**

Crear `C:\Users\be47\AppData\Local\Temp\test_weekly_report.py`:

```python
# tests/test_weekly_report.py
from unittest.mock import patch, MagicMock
from datetime import datetime
from zoneinfo import ZoneInfo
from app.db.models import Trade, Pattern


def make_trade(symbol="AAPL", pnl_usd=15.0, pnl_pct=0.04, exit_reason="TAKE_PROFIT"):
    return Trade(
        id=1, symbol=symbol, action="BUY", quantity=1,
        entry_price=280.0, stop_loss_price=273.0, take_profit_price=296.8,
        stop_loss_pct=0.025, take_profit_pct=0.06, signal_strength="STRONG",
        llm_justification="Test", status="CLOSED",
        exit_price=280.0 * 1.04, exit_reason=exit_reason,
        pnl_usd=pnl_usd, pnl_pct=pnl_pct,
        opened_at=datetime(2026, 5, 4, 10, 0, tzinfo=ZoneInfo("America/New_York")),
        closed_at=datetime(2026, 5, 5, 14, 0, tzinfo=ZoneInfo("America/New_York")),
        order_id="42",
    )


def test_generate_report_with_wins():
    from app.reports.weekly import generate_weekly_report
    trades = [make_trade("AAPL", 15.0, 0.04), make_trade("MSFT", -5.0, -0.02, "STOP_LOSS")]
    patterns = []
    report = generate_weekly_report(trades, patterns, capital=500.0)
    assert "AAPL" in report
    assert "MSFT" in report
    assert "10.00" in report  # net pnl
    assert "1 ganancias" in report or "wins" in report.lower()


def test_generate_report_no_trades():
    from app.reports.weekly import generate_weekly_report
    report = generate_weekly_report([], [], capital=500.0)
    assert "Sin operaciones" in report


def test_generate_report_includes_patterns():
    from app.reports.weekly import generate_weekly_report
    from app.db.models import Pattern
    now = datetime(2026, 5, 6, tzinfo=ZoneInfo("America/New_York"))
    patterns = [Pattern(id=1, symbol="AAPL", pattern_text="RSI<30 -> BUY confiable",
                        win_count=3, loss_count=1, created_at=now, updated_at=now)]
    report = generate_weekly_report([], patterns, capital=500.0)
    assert "RSI<30" in report


def test_generate_report_calculates_win_rate():
    from app.reports.weekly import generate_weekly_report
    trades = [
        make_trade("AAPL", 10.0, 0.03),
        make_trade("MSFT", 8.0, 0.025),
        make_trade("TSLA", -5.0, -0.02, "STOP_LOSS"),
    ]
    report = generate_weekly_report(trades, [], capital=500.0)
    assert "66" in report or "67" in report  # 2/3 win rate
```

- [ ] **Step 2: Crear app/reports/weekly.py LOCALMENTE**

Crear `C:\Users\be47\AppData\Local\Temp\weekly_report.py`:

```python
# app/reports/weekly.py
"""
Genera y envia el reporte semanal de trading cada lunes 8am ET.
Incluye: operaciones, P&L, win rate, patrones aprendidos.
"""
import logging
from datetime import datetime, timedelta
from zoneinfo import ZoneInfo

from app.notifications.telegram import notify

logger = logging.getLogger(__name__)
ET = ZoneInfo("America/New_York")


def generate_weekly_report(trades: list, patterns: list, capital: float) -> str:
    """
    Genera el texto del reporte semanal.
    trades: lista de Trade cerrados en la semana
    patterns: lista de Pattern aprendidos en la semana
    capital: capital simulado
    """
    if not trades:
        return (
            "Reporte Semanal IBKR AI Trader\n\n"
            "Sin operaciones esta semana.\n"
            "El scanner continua monitoreando el mercado."
        )

    wins = [t for t in trades if (t.pnl_usd or 0) > 0]
    losses = [t for t in trades if (t.pnl_usd or 0) <= 0]
    total_pnl = sum(t.pnl_usd or 0 for t in trades)
    win_rate = len(wins) / len(trades) * 100 if trades else 0

    # Simbolos mas activos
    symbols = {}
    for t in trades:
        symbols[t.symbol] = symbols.get(t.symbol, 0) + 1
    top_symbols = sorted(symbols.items(), key=lambda x: x[1], reverse=True)[:3]

    lines = [
        "Reporte Semanal IBKR AI Trader",
        "",
        f"Operaciones: {len(trades)} total | {len(wins)} ganancias | {len(losses)} perdidas",
        f"Win rate: {win_rate:.0f}%",
        f"P&L neto: ${total_pnl:.2f} ({total_pnl/capital*100:.1f}% del capital)",
        "",
        "Detalle:",
    ]

    for t in trades[:5]:  # max 5 para no saturar
        emoji = "+" if (t.pnl_usd or 0) >= 0 else "-"
        lines.append(
            f"  {emoji} {t.symbol} {t.action} | ${t.pnl_usd:.2f} ({t.exit_reason})"
        )

    if len(trades) > 5:
        lines.append(f"  ... y {len(trades) - 5} operaciones mas")

    if top_symbols:
        lines.append("")
        lines.append("Simbolos mas activos:")
        for sym, count in top_symbols:
            lines.append(f"  {sym}: {count} operacion(es)")

    if patterns:
        lines.append("")
        lines.append(f"Patrones aprendidos esta semana: {len(patterns)}")
        for p in patterns[:3]:
            lines.append(f"  - {p.pattern_text[:60]}")

    return "\n".join(lines)


def send_weekly_report(capital: float = 500.0):
    """Genera y envia el reporte semanal por Telegram."""
    from app.db.database import get_closed_trades, get_patterns_for_week

    now = datetime.now(tz=ET)
    week_ago = now - timedelta(days=7)

    try:
        trades = get_closed_trades_since(week_ago)
        patterns = get_patterns_for_week(week_ago)
    except Exception as e:
        logger.error(f"Could not fetch weekly data: {e}")
        trades, patterns = [], []

    report = generate_weekly_report(trades, patterns, capital)
    notify(report)
    logger.info("Weekly report sent")


def get_closed_trades_since(since: datetime) -> list:
    """Retorna trades cerrados desde una fecha."""
    from app.db.database import get_connection
    from app.db.models import Trade

    conn = get_connection()
    rows = conn.execute(
        "SELECT * FROM trades WHERE status='CLOSED' AND closed_at >= ? ORDER BY closed_at DESC",
        (since.isoformat(),)
    ).fetchall()
    conn.close()
    return [Trade(
        id=r["id"], symbol=r["symbol"], action=r["action"],
        quantity=r["quantity"], entry_price=r["entry_price"],
        stop_loss_price=r["stop_loss_price"], take_profit_price=r["take_profit_price"],
        stop_loss_pct=r["stop_loss_pct"], take_profit_pct=r["take_profit_pct"],
        signal_strength=r["signal_strength"], llm_justification=r["llm_justification"],
        status=r["status"], exit_price=r["exit_price"], exit_reason=r["exit_reason"],
        pnl_usd=r["pnl_usd"], pnl_pct=r["pnl_pct"],
        opened_at=datetime.fromisoformat(r["opened_at"]),
        closed_at=datetime.fromisoformat(r["closed_at"]) if r["closed_at"] else None,
        order_id=r["order_id"],
    ) for r in rows]
```

- [ ] **Step 3: Agregar get_patterns_for_week a database.py**

Agregar al final de database.py en la Pi:
```python
def get_patterns_for_week(since: datetime) -> list:
    """Retorna patrones creados desde una fecha."""
    conn = get_connection()
    rows = conn.execute(
        "SELECT * FROM patterns WHERE created_at >= ? ORDER BY created_at DESC",
        (since.isoformat(),)
    ).fetchall()
    conn.close()
    return [Pattern(
        id=r["id"], symbol=r["symbol"], pattern_text=r["pattern_text"],
        win_count=r["win_count"], loss_count=r["loss_count"],
        created_at=datetime.fromisoformat(r["created_at"]),
        updated_at=datetime.fromisoformat(r["updated_at"]),
    ) for r in rows]
```

- [ ] **Step 4: SCP archivos y correr tests en Pi**

```bash
scp C:\Users\be47\AppData\Local\Temp\weekly_report.py aiutox-pi:~/ibkr-bot/app/reports/weekly.py
scp C:\Users\be47\AppData\Local\Temp\test_weekly_report.py aiutox-pi:~/ibkr-bot/tests/test_weekly_report.py
ssh aiutox-pi "mkdir -p ~/ibkr-bot/app/reports && touch ~/ibkr-bot/app/reports/__init__.py"
ssh aiutox-pi "cd ~/ibkr-bot && source .venv/bin/activate && pytest tests/test_weekly_report.py -v 2>&1"
```
Expected: 4 PASS

- [ ] **Step 5: Agregar job semanal a run.py**

En run.py agregar import y job:
```python
from app.reports.weekly import send_weekly_report
from app.config.settings import SIMULATED_CAPITAL

# En scheduler, agregar:
scheduler.add_job(
    lambda: send_weekly_report(SIMULATED_CAPITAL),
    "cron",
    day_of_week="mon",
    hour=8,
    minute=0,
    timezone=MARKET_TZ,
    id="weekly_report",
)
```

- [ ] **Step 6: Commit**

```bash
ssh aiutox-pi "cd ~/ibkr-bot && git add app/reports/ app/db/database.py run.py tests/test_weekly_report.py && git commit -m 'feat: weekly Telegram report every Monday 8am ET'"
```

---

## Task 3: Dashboard web simple

**Files:**
- Create: `app/api/dashboard.py`
- Modify: `app/api/main.py` — agregar GET /dashboard

El dashboard es HTML puro generado por Python. Sin CSS frameworks, sin JS externo. Legible en movil. Se accede desde `http://192.168.x.x:8088/dashboard` en la red local.

- [ ] **Step 1: Escribir tests del generador HTML LOCALMENTE**

Crear `C:\Users\be47\AppData\Local\Temp\test_dashboard.py`:

```python
# tests/test_dashboard.py
from app.api.dashboard import render_dashboard


def test_dashboard_contains_system_status():
    html = render_dashboard(
        status={"mode": "paper", "paused": False, "daily_pnl_usd": 5.0,
                "daily_pnl_pct": 1.0, "open_positions": 1, "simulated_capital": 500},
        trades=[],
        closed_trades=[],
        signals=[],
        patterns=[],
    )
    assert "paper" in html.lower()
    assert "500" in html


def test_dashboard_shows_open_positions():
    html = render_dashboard(
        status={"mode": "paper", "paused": False, "daily_pnl_usd": 0,
                "daily_pnl_pct": 0, "open_positions": 1, "simulated_capital": 500},
        trades=[{"symbol": "AAPL", "action": "BUY", "quantity": 1,
                  "entry_price": 287.0, "stop_loss_price": 280.0,
                  "take_profit_price": 304.0, "signal_strength": "STRONG",
                  "status": "OPEN", "opened_at": "2026-05-06T10:00:00"}],
        closed_trades=[],
        signals=[],
        patterns=[],
    )
    assert "AAPL" in html
    assert "287" in html


def test_dashboard_shows_signals():
    html = render_dashboard(
        status={"mode": "paper", "paused": False, "daily_pnl_usd": 0,
                "daily_pnl_pct": 0, "open_positions": 0, "simulated_capital": 500},
        trades=[],
        closed_trades=[],
        signals=[{"symbol": "TSLA", "strength": "STRONG", "rsi": 28.5,
                   "macd": -0.12, "volume_ratio": 1.8,
                   "created_at": "2026-05-06T10:00:00"}],
        patterns=[],
    )
    assert "TSLA" in html
    assert "STRONG" in html


def test_dashboard_is_valid_html():
    html = render_dashboard(
        status={"mode": "paper", "paused": False, "daily_pnl_usd": 0,
                "daily_pnl_pct": 0, "open_positions": 0, "simulated_capital": 500},
        trades=[], closed_trades=[], signals=[], patterns=[],
    )
    assert html.startswith("<!DOCTYPE html>")
    assert "</html>" in html
```

- [ ] **Step 2: Crear app/api/dashboard.py LOCALMENTE**

Crear `C:\Users\be47\AppData\Local\Temp\dashboard.py`:

```python
# app/api/dashboard.py
"""
Genera el HTML del dashboard de trading.
HTML puro sin frameworks — legible en movil.
Accesible en http://<pi-ip>:8088/dashboard
"""


def _row(cells: list, header: bool = False) -> str:
    tag = "th" if header else "td"
    inner = "".join(f"<{tag}>{c}</{tag}>" for c in cells)
    return f"<tr>{inner}</tr>"


def _table(headers: list, rows: list) -> str:
    head = _row(headers, header=True)
    body = "".join(_row(r) for r in rows)
    return f"<table><thead>{head}</thead><tbody>{body}</tbody></table>"


def _color(value: float) -> str:
    return "green" if value >= 0 else "red"


def render_dashboard(
    status: dict,
    trades: list,
    closed_trades: list,
    signals: list,
    patterns: list,
) -> str:
    mode = status.get("mode", "paper").upper()
    paused = "PAUSADO" if status.get("paused") else "Activo"
    daily_pnl = status.get("daily_pnl_usd", 0.0)
    daily_pct = status.get("daily_pnl_pct", 0.0)
    capital = status.get("simulated_capital", 500)
    open_pos = status.get("open_positions", 0)
    pnl_color = _color(daily_pnl)

    # Posiciones abiertas
    if trades:
        trade_rows = [
            [t["symbol"], t["action"], t["quantity"],
             f"${t['entry_price']:.2f}", f"${t['stop_loss_price']:.2f}",
             f"${t['take_profit_price']:.2f}", t["signal_strength"]]
            for t in trades
        ]
        trades_html = _table(
            ["Simbolo", "Accion", "Qty", "Entrada", "SL", "TP", "Senal"],
            trade_rows
        )
    else:
        trades_html = "<p>Sin posiciones abiertas</p>"

    # Historial reciente
    if closed_trades:
        hist_rows = [
            [t["symbol"], t["action"],
             f'<span style="color:{_color(t.get("pnl_usd") or 0)}">${t.get("pnl_usd") or 0:.2f}</span>',
             t.get("exit_reason", "?"),
             (t.get("closed_at") or "")[:16]]
            for t in closed_trades[:5]
        ]
        hist_html = _table(["Simbolo", "Accion", "P&L", "Razon", "Fecha"], hist_rows)
    else:
        hist_html = "<p>Sin historial</p>"

    # Señales recientes
    if signals:
        sig_rows = [
            [s["symbol"], f'<b>{s["strength"]}</b>',
             s.get("rsi", "?"), s.get("volume_ratio", "?"),
             (s.get("created_at") or "")[:16]]
            for s in signals[:5]
        ]
        sig_html = _table(["Simbolo", "Fuerza", "RSI", "Vol", "Hora"], sig_rows)
    else:
        sig_html = "<p>Sin señales recientes</p>"

    # Patrones
    if patterns:
        pat_rows = [[p.get("symbol", "?"), p.get("pattern", "?"),
                     p.get("wins", 0), p.get("losses", 0)]
                    for p in patterns[:5]]
        pat_html = _table(["Simbolo", "Patron", "Wins", "Losses"], pat_rows)
    else:
        pat_html = "<p>Sin patrones aprendidos aun</p>"

    return f"""<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<meta http-equiv="refresh" content="60">
<title>IBKR AI Trader</title>
<style>
  body {{ font-family: monospace; margin: 16px; background: #111; color: #eee; }}
  h1 {{ color: #4af; margin-bottom: 4px; }}
  h2 {{ color: #aaa; border-bottom: 1px solid #333; padding-bottom: 4px; }}
  .status {{ background: #1a1a2e; padding: 12px; border-radius: 6px; margin-bottom: 16px; }}
  .stat {{ display: inline-block; margin-right: 24px; }}
  .stat span {{ font-size: 1.4em; font-weight: bold; }}
  table {{ width: 100%; border-collapse: collapse; margin-bottom: 16px; font-size: 0.85em; }}
  th {{ background: #222; color: #4af; padding: 6px; text-align: left; }}
  td {{ padding: 5px; border-bottom: 1px solid #222; }}
  tr:hover {{ background: #1a1a1a; }}
  .badge-paper {{ background: #1a3a1a; color: #4f4; padding: 2px 8px; border-radius: 4px; }}
  .badge-live {{ background: #3a1a1a; color: #f44; padding: 2px 8px; border-radius: 4px; }}
  p {{ color: #666; }}
</style>
</head>
<body>
<h1>IBKR AI Trader</h1>
<div class="status">
  <div class="stat">Modo: <span class="badge-{'paper' if mode == 'PAPER' else 'live'}">{mode}</span></div>
  <div class="stat">Estado: <span>{paused}</span></div>
  <div class="stat">Capital: <span>${capital}</span></div>
  <div class="stat">P&amp;L hoy: <span style="color:{pnl_color}">${daily_pnl:.2f} ({daily_pct:.1f}%)</span></div>
  <div class="stat">Posiciones: <span>{open_pos}/3</span></div>
</div>

<h2>Posiciones Abiertas</h2>
{trades_html}

<h2>Historial Reciente</h2>
{hist_html}

<h2>Senales Detectadas</h2>
{sig_html}

<h2>Patrones Aprendidos</h2>
{pat_html}

<p style="color:#444; font-size:0.8em">Actualiza cada 60s — {__import__('datetime').datetime.now().strftime('%Y-%m-%d %H:%M')}</p>
</body>
</html>"""
```

- [ ] **Step 3: SCP y correr tests**

```bash
scp C:\Users\be47\AppData\Local\Temp\dashboard.py aiutox-pi:~/ibkr-bot/app/api/dashboard.py
scp C:\Users\be47\AppData\Local\Temp\test_dashboard.py aiutox-pi:~/ibkr-bot/tests/test_dashboard.py
ssh aiutox-pi "cd ~/ibkr-bot && source .venv/bin/activate && pytest tests/test_dashboard.py -v 2>&1"
```
Expected: 4 PASS

- [ ] **Step 4: Agregar endpoint /dashboard a main.py**

Agregar al final de app/api/main.py:
```python
@app.get("/dashboard", response_class=HTMLResponse)
def dashboard():
    from fastapi.responses import HTMLResponse
    from app.api.dashboard import render_dashboard
    from app.db.database import get_open_trades, get_closed_trades, get_pending_signals, get_patterns_for_symbol
    from app.config.settings import SIMULATED_CAPITAL, ALLOWED_SYMBOLS

    try:
        status_data = {
            "mode": "paper",
            "paused": False,
            "daily_pnl_usd": get_daily_pnl(),
            "daily_pnl_pct": round(get_daily_pnl() / SIMULATED_CAPITAL * 100, 2),
            "open_positions": len(get_open_trades()),
            "simulated_capital": SIMULATED_CAPITAL,
        }
        try:
            from app.system.controller import get_controller
            ctrl = get_controller()
            status_data["mode"] = ctrl.mode
            status_data["paused"] = ctrl.is_paused
        except RuntimeError:
            pass
    except Exception:
        status_data = {"mode": "paper", "paused": False, "daily_pnl_usd": 0,
                       "daily_pnl_pct": 0, "open_positions": 0, "simulated_capital": SIMULATED_CAPITAL}

    trades = [
        {"symbol": t.symbol, "action": t.action, "quantity": t.quantity,
         "entry_price": t.entry_price, "stop_loss_price": t.stop_loss_price,
         "take_profit_price": t.take_profit_price, "signal_strength": t.signal_strength,
         "status": t.status, "opened_at": t.opened_at.isoformat()}
        for t in get_open_trades()
    ]
    closed = [
        {"symbol": t.symbol, "action": t.action, "pnl_usd": t.pnl_usd,
         "exit_reason": t.exit_reason, "closed_at": t.closed_at.isoformat() if t.closed_at else None}
        for t in get_closed_trades(limit=5)
    ]
    signals = [
        {"symbol": s.symbol, "strength": s.strength, "rsi": s.rsi,
         "volume_ratio": s.volume_ratio, "created_at": s.created_at.isoformat()}
        for s in get_pending_signals()
    ]
    all_patterns = []
    for sym in ALLOWED_SYMBOLS[:5]:
        for p in get_patterns_for_symbol(sym)[:1]:
            all_patterns.append({"symbol": p.symbol, "pattern": p.pattern_text,
                                  "wins": p.win_count, "losses": p.loss_count})

    html = render_dashboard(status_data, trades, closed, signals, all_patterns)
    return HTMLResponse(content=html)
```

Also add `from app.db.database import get_daily_pnl` to imports at top of main.py.

- [ ] **Step 5: Reiniciar y verificar dashboard**

```bash
ssh aiutox-pi "kill \$(ps aux | grep 'run.py' | grep -v grep | awk '{print \$2}') 2>/dev/null; sleep 2"
ssh aiutox-pi "cd ~/ibkr-bot && source .venv/bin/activate && nohup python3 run.py > /tmp/run.log 2>&1 & sleep 10"
ssh aiutox-pi "curl -s http://127.0.0.1:8088/dashboard | grep -o '<title>.*</title>'"
```
Expected: `<title>IBKR AI Trader</title>`

Get Pi IP for phone access:
```bash
ssh aiutox-pi "hostname -I | awk '{print $1}'"
```
Open `http://<pi-ip>:8088/dashboard` in phone browser on same WiFi network.

- [ ] **Step 6: Commit**

```bash
ssh aiutox-pi "cd ~/ibkr-bot && git add app/api/dashboard.py app/api/main.py tests/test_dashboard.py && git commit -m 'feat: web dashboard at /dashboard with auto-refresh every 60s'"
```

---

## Task 4: Estrategias por categoria de simbolo

**Files:**
- Modify: `app/db/database.py` — agregar category a symbol_config
- Modify: `app/llm/agent.py` — prompts especializados por categoria
- Create: `tests/test_agent_strategies.py`

**Categorias:** `etf` (SPY, QQQ), `blue_chip` (AAPL, MSFT, GOOGL, AMZN, META, JPM), `growth` (TSLA, NVDA)

Cada categoria tiene un contexto diferente en el prompt:
- **ETF:** conservador, sigue tendencia macro, stop-loss mas ajustado
- **Blue chip:** moderado, fundamentals importan, paciencia en swings
- **Growth:** agresivo, volatilidad alta, stop-loss mas amplio

- [ ] **Step 1: Escribir tests LOCALMENTE**

Crear `C:\Users\be47\AppData\Local\Temp\test_agent_strategies.py`:

```python
# tests/test_agent_strategies.py
from app.llm.agent import get_symbol_category, get_strategy_context


def test_etf_category():
    assert get_symbol_category("SPY") == "etf"
    assert get_symbol_category("QQQ") == "etf"


def test_blue_chip_category():
    assert get_symbol_category("AAPL") == "blue_chip"
    assert get_symbol_category("MSFT") == "blue_chip"
    assert get_symbol_category("JPM") == "blue_chip"


def test_growth_category():
    assert get_symbol_category("TSLA") == "growth"
    assert get_symbol_category("NVDA") == "growth"


def test_unknown_defaults_to_blue_chip():
    assert get_symbol_category("FAKE") == "blue_chip"


def test_etf_context_mentions_conservative():
    ctx = get_strategy_context("etf")
    assert "conservador" in ctx.lower() or "macro" in ctx.lower()


def test_growth_context_mentions_volatility():
    ctx = get_strategy_context("growth")
    assert "volatil" in ctx.lower() or "agresiv" in ctx.lower()


def test_blue_chip_context_is_moderate():
    ctx = get_strategy_context("blue_chip")
    assert "moderado" in ctx.lower() or "fundamental" in ctx.lower()
```

- [ ] **Step 2: Correr tests localmente para ver que fallan**

Note: estos tests requieren que agent.py tenga las nuevas funciones. Los correremos en la Pi despues del SCP.

- [ ] **Step 3: Agregar funciones a agent.py en la Pi**

Leer agent.py:
```bash
ssh aiutox-pi "head -30 ~/ibkr-bot/app/llm/agent.py"
```

Agregar despues de los imports y constantes en agent.py:

```python
# Categorias de simbolos y sus estrategias
SYMBOL_CATEGORIES = {
    "etf": ["SPY", "QQQ"],
    "blue_chip": ["AAPL", "MSFT", "GOOGL", "AMZN", "META", "JPM"],
    "growth": ["TSLA", "NVDA"],
}

STRATEGY_CONTEXTS = {
    "etf": (
        "Este simbolo es un ETF. Estrategia conservadora: "
        "sigue la tendencia macro, evita entrar contra la tendencia del mercado. "
        "Stop-loss mas ajustado (1.5-2%). Solo entrar con señal STRONG en los 3 timeframes."
    ),
    "blue_chip": (
        "Este simbolo es una empresa blue chip solida. Estrategia moderada: "
        "los fundamentales son importantes, los swings pueden durar mas tiempo. "
        "Stop-loss moderado (2-3%). Puedes entrar con señal MEDIUM si los patrones son consistentes."
    ),
    "growth": (
        "Este simbolo es una accion de alto crecimiento y alta volatilidad. Estrategia mas agresiva: "
        "los movimientos son mas bruscos, la volatilidad es alta. "
        "Stop-loss mas amplio (2.5-4%) para evitar salidas prematuras. "
        "Solo entrar con señal STRONG y noticias neutrales o positivas."
    ),
}


def get_symbol_category(symbol: str) -> str:
    """Retorna la categoria del simbolo: etf, blue_chip, o growth."""
    sym = symbol.upper()
    for category, symbols in SYMBOL_CATEGORIES.items():
        if sym in symbols:
            return category
    return "blue_chip"  # default para simbolos no clasificados


def get_strategy_context(category: str) -> str:
    """Retorna el contexto de estrategia para una categoria."""
    return STRATEGY_CONTEXTS.get(category, STRATEGY_CONTEXTS["blue_chip"])
```

Y modificar `analyze_signal()` para incluir el contexto de estrategia en el prompt. Buscar la linea donde empieza el prompt y agregar despues de la primera linea:

```python
    # Agregar contexto de estrategia segun la categoria del simbolo
    category = get_symbol_category(symbol)
    strategy_ctx = get_strategy_context(category)
```

Y en el prompt agregar una seccion:
```
ESTRATEGIA PARA {category.upper()}:
{strategy_ctx}
```

- [ ] **Step 4: SCP tests y correr en Pi**

```bash
scp C:\Users\be47\AppData\Local\Temp\test_agent_strategies.py aiutox-pi:~/ibkr-bot/tests/test_agent_strategies.py
ssh aiutox-pi "cd ~/ibkr-bot && source .venv/bin/activate && pytest tests/test_agent_strategies.py -v 2>&1"
```
Expected: 7 PASS

- [ ] **Step 5: Commit**

```bash
ssh aiutox-pi "cd ~/ibkr-bot && git add app/llm/agent.py tests/test_agent_strategies.py && git commit -m 'feat: symbol-specific trading strategies (etf/blue_chip/growth)'"
```

---

## Task 5: Verificacion final Plan 2

- [ ] **Step 1: Correr suite completa de tests**

```bash
ssh aiutox-pi "cd ~/ibkr-bot && source .venv/bin/activate && pytest tests/ --ignore=tests/test_ibkr_client.py -v 2>&1 | tail -25"
```
Expected: 63+ PASS (52 previos + 4 alerts + 4 weekly + 4 dashboard + 7 strategies)

- [ ] **Step 2: Verificar todos los endpoints nuevos**

```bash
ssh aiutox-pi "curl -s http://127.0.0.1:8088/system/status | python3 -m json.tool"
ssh aiutox-pi "curl -s http://127.0.0.1:8088/dashboard | grep '<title>'"
ssh aiutox-pi "curl -s http://127.0.0.1:8088/trades/closed | python3 -m json.tool | head -10"
```

- [ ] **Step 3: Git log final**

```bash
ssh aiutox-pi "cd ~/ibkr-bot && git log --oneline -10"
```

---

## Self-Review

**Cobertura:**
- ✅ Alertas de precio /alerta SYMBOL UMBRAL — Task 1
- ✅ /alertas lista alertas activas — Task 1
- ✅ /eliminar_alerta ID — Task 1
- ✅ check_all_alerts en scheduler cada 2 min — Task 1
- ✅ Reporte semanal lunes 8am ET — Task 2
- ✅ generate_weekly_report con trades, patterns, win rate — Task 2
- ✅ Dashboard web HTML en /dashboard — Task 3
- ✅ Auto-refresh cada 60s — Task 3
- ✅ get_symbol_category() y get_strategy_context() — Task 4
- ✅ Prompts especializados etf/blue_chip/growth — Task 4
- ✅ Tests para todo — Tasks 1-4

**Consistencia:**
- AlertConfig.threshold_pct es float decimal (0.05 = 5%) en todos los usos ✅
- generate_weekly_report recibe list[Trade] y list[Pattern] ✅
- get_symbol_category retorna str literal "etf"|"blue_chip"|"growth" ✅
- render_dashboard recibe dicts simples (no dataclasses) ✅
- get_active_alerts retorna list[AlertConfig] ✅
