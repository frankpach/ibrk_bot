# IBKR AI Trader v2 — Plan 1: Fundamentos + Telegram Bot

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Completar el sistema con Telegram bot bidireccional, multi-timeframe analysis, news/sentiment, system controller, reconciliación DB vs IB, y circuit breaker diario.

**Architecture:** Telegram bot corre como daemon en thread separado dentro de run.py. Comandos directos van a FastAPI sin pasar por LLM. Comandos de análisis llaman a OpenCode via subprocess con las MCP tools. Multi-timeframe reemplaza el scanner actual usando barras daily+hourly+5min. News se obtiene via RSS feedparser. System controller expone endpoints FastAPI para control en caliente.

**Tech Stack:** Python 3.13, python-telegram-bot 20.7, feedparser, APScheduler, FastAPI, OpenCode (opencode-go/qwen3.5-plus), httpx, SQLite

---

## Mapa de Archivos

```
~/ibkr-bot/
├── app/
│   ├── scanner/
│   │   ├── preprocessor.py       MODIFICAR — multi-timeframe (daily+hourly+5min)
│   │   └── news.py               CREAR — RSS Yahoo Finance, últimas noticias por símbolo
│   ├── system/
│   │   ├── __init__.py           CREAR
│   │   └── controller.py         CREAR — pause/resume/mode/circuit_breaker
│   ├── notifications/
│   │   ├── telegram.py           ya existe — notify(), request_approval()
│   │   └── telegram_bot.py       CREAR — daemon comandos directos + LLM handler
│   └── api/
│       └── main.py               MODIFICAR — nuevos endpoints sistema + trades/closed
├── run.py                        MODIFICAR — iniciar bot + reconciliación + circuit breaker
└── tests/
    ├── test_multitimeframe.py    CREAR
    ├── test_news.py              CREAR
    ├── test_system_controller.py CREAR
    └── test_telegram_bot.py      CREAR
```

---

## Task 1: Cerrar posición AAPL de prueba + reconciliación DB vs IB

**Files:**
- Modify: `~/ibkr-bot/app/db/database.py`
- Modify: `~/ibkr-bot/app/ibkr/client.py`
- Create: `~/ibkr-bot/app/system/reconciler.py`

- [ ] **Step 1: Cerrar la posición de prueba en DB**

```bash
ssh aiutox-pi "cd ~/ibkr-bot && source .venv/bin/activate && python3 -c \"
from app.db.database import get_open_trades, close_trade
from datetime import datetime
trades = get_open_trades()
print('Open trades:', [(t.id, t.symbol, t.quantity) for t in trades])
for t in trades:
    if t.llm_justification == 'Placed via MCP':
        close_trade(t.id, t.entry_price, 'CANCELLED_TEST', 0.0, 0.0)
        print(f'Closed test trade {t.id} {t.symbol}')
\""
```
Esperado: `Closed test trade 1 AAPL`

- [ ] **Step 2: Agregar get_closed_trades a database.py**

Leer el final de database.py:
```bash
ssh aiutox-pi "tail -20 ~/ibkr-bot/app/db/database.py"
```

Agregar al final:
```python
def get_closed_trades(limit: int = 10) -> list:
    """Retorna los últimos N trades cerrados."""
    conn = get_connection()
    rows = conn.execute(
        "SELECT * FROM trades WHERE status='CLOSED' ORDER BY closed_at DESC LIMIT ?",
        (limit,)
    ).fetchall()
    conn.close()
    return [Trade(
        id=r["id"], symbol=r["symbol"], action=r["action"],
        quantity=r["quantity"], entry_price=r["entry_price"],
        stop_loss_price=r["stop_loss_price"], take_profit_price=r["take_profit_price"],
        stop_loss_pct=r["stop_loss_pct"], take_profit_pct=r["take_profit_pct"],
        signal_strength=r["signal_strength"], llm_justification=r["llm_justification"],
        status=r["status"], exit_price=r["exit_price"], exit_reason=r["exit_reason"],
        pnl_usd=r["pnl_usd"], pnl_pct=r["pnl_pct"],
        opened_at=datetime.fromisoformat(r["opened_at"]),
        closed_at=datetime.fromisoformat(r["closed_at"]) if r["closed_at"] else None,
        order_id=r["order_id"],
    ) for r in rows]


def get_daily_pnl() -> float:
    """Retorna el P&L total de trades cerrados hoy."""
    conn = get_connection()
    today = datetime.utcnow().date().isoformat()
    rows = conn.execute(
        "SELECT COALESCE(SUM(pnl_usd), 0) as total FROM trades WHERE status='CLOSED' AND closed_at >= ?",
        (today,)
    ).fetchone()
    conn.close()
    return float(rows["total"])
```

- [ ] **Step 3: Crear app/system/__init__.py**

```bash
ssh aiutox-pi "mkdir -p ~/ibkr-bot/app/system && touch ~/ibkr-bot/app/system/__init__.py"
```

- [ ] **Step 4: Crear app/system/reconciler.py**

```python
# app/system/reconciler.py
"""
Reconcilia posiciones abiertas en IB Gateway vs las registradas en la DB.
Se ejecuta al arrancar el sistema para detectar desincronizaciones.
"""
import logging
from app.db.database import get_open_trades, close_trade
from app.notifications.telegram import notify

logger = logging.getLogger(__name__)


def reconcile_positions(ib_client) -> int:
    """
    Compara posiciones en DB vs IB real.
    Si una posición está en DB pero no en IB, la marca como cerrada.
    Retorna el número de posiciones reconciliadas.
    """
    db_trades = get_open_trades()
    if not db_trades:
        return 0

    try:
        ib_portfolio = ib_client.get_portfolio()
        ib_symbols = {p["symbol"] for p in ib_portfolio if abs(p["quantity"]) > 0}
    except Exception as e:
        logger.error(f"Could not fetch IB portfolio for reconciliation: {e}")
        return 0

    reconciled = 0
    for trade in db_trades:
        if trade.symbol not in ib_symbols:
            logger.warning(
                f"Trade {trade.id} {trade.symbol} in DB but not in IB — marking as closed"
            )
            close_trade(
                trade_id=trade.id,
                exit_price=trade.entry_price,
                exit_reason="RECONCILED_NOT_IN_IB",
                pnl_usd=0.0,
                pnl_pct=0.0,
            )
            reconciled += 1

    if reconciled > 0:
        notify(
            f"Reconciliacion: {reconciled} posicion(es) en DB no encontradas en IB.\n"
            f"Marcadas como cerradas automaticamente."
        )
        logger.info(f"Reconciled {reconciled} positions")

    return reconciled
```

- [ ] **Step 5: Verificar que no hay trades abiertos de prueba**

```bash
ssh aiutox-pi "cd ~/ibkr-bot && source .venv/bin/activate && python3 -c 'from app.db.database import get_open_trades; t=get_open_trades(); print(\"Open:\", len(t), [(x.id, x.symbol) for x in t])'"
```
Esperado: `Open: 0 []`

- [ ] **Step 6: Commit**

```bash
ssh aiutox-pi "cd ~/ibkr-bot && git add app/db/database.py app/system/ && git commit -m 'feat: close test positions, add reconciler, get_closed_trades, get_daily_pnl'"
```

---

## Task 2: System Controller (pause/resume/mode/circuit breaker)

**Files:**
- Create: `~/ibkr-bot/app/system/controller.py`
- Modify: `~/ibkr-bot/app/api/main.py`
- Create: `~/ibkr-bot/tests/test_system_controller.py`

- [ ] **Step 1: Escribir tests**

```python
# tests/test_system_controller.py
from unittest.mock import MagicMock, patch
from app.system.controller import SystemController


def test_pause_stops_scanner():
    mock_scheduler = MagicMock()
    ctrl = SystemController(mock_scheduler)
    ctrl.pause()
    mock_scheduler.pause_job.assert_called_with("scanner")
    assert ctrl.is_paused is True


def test_resume_restarts_scanner():
    mock_scheduler = MagicMock()
    ctrl = SystemController(mock_scheduler)
    ctrl.pause()
    ctrl.resume()
    mock_scheduler.resume_job.assert_called_with("scanner")
    assert ctrl.is_paused is False


def test_circuit_breaker_triggers_on_loss():
    mock_scheduler = MagicMock()
    ctrl = SystemController(mock_scheduler)
    # $25 loss on $500 capital = 5% = triggers breaker
    triggered = ctrl.check_circuit_breaker(daily_pnl=-25.0, capital=500.0)
    assert triggered is True
    assert ctrl.is_paused is True


def test_circuit_breaker_does_not_trigger_on_small_loss():
    mock_scheduler = MagicMock()
    ctrl = SystemController(mock_scheduler)
    triggered = ctrl.check_circuit_breaker(daily_pnl=-10.0, capital=500.0)
    assert triggered is False
    assert ctrl.is_paused is False


def test_mode_toggle():
    mock_scheduler = MagicMock()
    ctrl = SystemController(mock_scheduler)
    assert ctrl.mode == "paper"
    ctrl.set_mode("live")
    assert ctrl.mode == "live"
    ctrl.set_mode("paper")
    assert ctrl.mode == "paper"
```

- [ ] **Step 2: Correr tests — verificar que fallan**

```bash
ssh aiutox-pi "cd ~/ibkr-bot && source .venv/bin/activate && pytest tests/test_system_controller.py -v 2>&1 | tail -5"
```
Esperado: `ModuleNotFoundError`

- [ ] **Step 3: Crear app/system/controller.py**

```python
# app/system/controller.py
"""
Controlador del sistema: pause/resume, cambio de modo paper/live, circuit breaker.
Instancia única compartida entre el bot de Telegram y la API.
"""
import logging
from app.config.settings import SIMULATED_CAPITAL
from app.notifications.telegram import notify

logger = logging.getLogger(__name__)

CIRCUIT_BREAKER_PCT = 0.05  # 5% de pérdida diaria detiene el sistema


class SystemController:
    def __init__(self, scheduler):
        self.scheduler = scheduler
        self.is_paused = False
        self.mode = "paper"

    def pause(self):
        self.scheduler.pause_job("scanner")
        self.scheduler.pause_job("signal_processor")
        self.is_paused = True
        logger.info("System paused")

    def resume(self):
        self.scheduler.resume_job("scanner")
        self.scheduler.resume_job("signal_processor")
        self.is_paused = False
        logger.info("System resumed")

    def set_mode(self, mode: str):
        if mode not in ("paper", "live"):
            raise ValueError(f"Invalid mode: {mode}")
        import app.config.settings as s
        if mode == "live":
            s.PAPER_TRADING_ONLY = False
            s.REQUIRE_HUMAN_APPROVAL = True
        else:
            s.PAPER_TRADING_ONLY = True
            s.REQUIRE_HUMAN_APPROVAL = False
        self.mode = mode
        logger.info(f"Mode changed to {mode}")

    def check_circuit_breaker(self, daily_pnl: float, capital: float) -> bool:
        """Retorna True si la pérdida diaria supera el umbral y pausa el sistema."""
        if daily_pnl >= 0:
            return False
        loss_pct = abs(daily_pnl) / capital
        if loss_pct >= CIRCUIT_BREAKER_PCT:
            self.pause()
            notify(
                f"CIRCUIT BREAKER ACTIVADO\n"
                f"Perdida del dia: ${abs(daily_pnl):.2f} ({loss_pct:.1%})\n"
                f"Limite: {CIRCUIT_BREAKER_PCT:.0%} de ${capital}\n"
                f"Sistema pausado. Usa /reanudar para continuar."
            )
            logger.warning(f"Circuit breaker triggered: {loss_pct:.1%} daily loss")
            return True
        return False

    def status(self) -> dict:
        return {
            "paused": self.is_paused,
            "mode": self.mode,
            "circuit_breaker_threshold": f"{CIRCUIT_BREAKER_PCT:.0%}",
        }


# Instancia global — inicializada en run.py
_controller: SystemController | None = None


def get_controller() -> SystemController:
    if _controller is None:
        raise RuntimeError("SystemController not initialized. Call init_controller() first.")
    return _controller


def init_controller(scheduler) -> SystemController:
    global _controller
    _controller = SystemController(scheduler)
    return _controller
```

- [ ] **Step 4: Correr tests**

```bash
ssh aiutox-pi "cd ~/ibkr-bot && source .venv/bin/activate && pytest tests/test_system_controller.py -v 2>&1"
```
Esperado: 5 PASS

- [ ] **Step 5: Agregar endpoints de sistema a main.py**

Agregar al final de `app/api/main.py`:
```python
# --- System endpoints ---

@app.get("/system/status")
def system_status():
    from app.system.controller import get_controller
    from app.db.database import get_daily_pnl, get_open_trades
    from app.config.settings import SIMULATED_CAPITAL
    try:
        ctrl = get_controller()
        status = ctrl.status()
    except RuntimeError:
        status = {"paused": False, "mode": "paper"}
    open_trades = get_open_trades()
    daily_pnl = get_daily_pnl()
    return {
        **status,
        "open_positions": len(open_trades),
        "daily_pnl_usd": round(daily_pnl, 2),
        "daily_pnl_pct": round(daily_pnl / SIMULATED_CAPITAL * 100, 2),
        "simulated_capital": SIMULATED_CAPITAL,
    }


@app.post("/system/pause")
def system_pause():
    from app.system.controller import get_controller
    get_controller().pause()
    return {"status": "paused"}


@app.post("/system/resume")
def system_resume():
    from app.system.controller import get_controller
    get_controller().resume()
    return {"status": "resumed"}


@app.post("/system/mode/{mode}")
def system_mode(mode: str):
    from app.system.controller import get_controller
    try:
        get_controller().set_mode(mode)
        return {"status": "ok", "mode": mode}
    except ValueError as e:
        raise HTTPException(status_code=400, detail=str(e))


@app.get("/trades/closed")
def get_closed_trades_endpoint(limit: int = 10):
    from app.db.database import get_closed_trades
    trades = get_closed_trades(limit)
    return [
        {
            "id": t.id, "symbol": t.symbol, "action": t.action,
            "quantity": t.quantity, "entry_price": t.entry_price,
            "exit_price": t.exit_price, "exit_reason": t.exit_reason,
            "pnl_usd": t.pnl_usd, "pnl_pct": t.pnl_pct,
            "opened_at": t.opened_at.isoformat(),
            "closed_at": t.closed_at.isoformat() if t.closed_at else None,
        }
        for t in trades
    ]


@app.post("/orders/close/{symbol}")
def close_position(symbol: str):
    """Cierra una posición abierta inmediatamente a precio de mercado."""
    from app.db.database import get_open_trades, close_trade
    import httpx
    symbol = symbol.upper()
    trades = [t for t in get_open_trades() if t.symbol == symbol]
    if not trades:
        raise HTTPException(status_code=404, detail=f"No open position for {symbol}")
    trade = trades[0]
    try:
        price_data = client.get_stock_price(symbol)
        current_price = price_data["market_price"]
    except Exception as e:
        raise HTTPException(status_code=500, detail=f"Could not fetch price: {e}")
    pnl_pct = (current_price - trade.entry_price) / trade.entry_price
    if trade.action == "SELL":
        pnl_pct = -pnl_pct
    pnl_usd = pnl_pct * trade.entry_price * trade.quantity
    close_trade(trade.id, current_price, "MANUAL_CLOSE", round(pnl_usd, 2), round(pnl_pct, 4))
    return {
        "status": "closed", "symbol": symbol,
        "exit_price": current_price, "pnl_usd": round(pnl_usd, 2),
        "pnl_pct": round(pnl_pct * 100, 2),
    }


@app.post("/orders/close-all")
def close_all_positions():
    """Cierra todas las posiciones abiertas."""
    from app.db.database import get_open_trades, close_trade
    trades = get_open_trades()
    if not trades:
        return {"status": "ok", "closed": 0}
    closed = []
    for trade in trades:
        try:
            price_data = client.get_stock_price(trade.symbol)
            current_price = price_data["market_price"]
            pnl_pct = (current_price - trade.entry_price) / trade.entry_price
            if trade.action == "SELL":
                pnl_pct = -pnl_pct
            pnl_usd = pnl_pct * trade.entry_price * trade.quantity
            close_trade(trade.id, current_price, "MANUAL_CLOSE_ALL", round(pnl_usd, 2), round(pnl_pct, 4))
            closed.append({"symbol": trade.symbol, "pnl_usd": round(pnl_usd, 2)})
        except Exception as e:
            logger.error(f"Could not close {trade.symbol}: {e}")
    return {"status": "ok", "closed": len(closed), "positions": closed}
```

- [ ] **Step 6: Reiniciar y verificar endpoints**

```bash
ssh aiutox-pi "kill \$(ps aux | grep 'run.py' | grep -v grep | awk '{print \$2}') 2>/dev/null; sleep 2"
ssh aiutox-pi "cd ~/ibkr-bot && source .venv/bin/activate && nohup python3 run.py > /tmp/run.log 2>&1 & sleep 8 && curl -s http://127.0.0.1:8088/system/status | python3 -m json.tool"
```
Esperado:
```json
{"paused": false, "mode": "paper", "open_positions": 0, "daily_pnl_usd": 0.0, ...}
```

- [ ] **Step 7: Commit**

```bash
ssh aiutox-pi "cd ~/ibkr-bot && git add app/system/controller.py app/api/main.py tests/test_system_controller.py && git commit -m 'feat: add system controller, circuit breaker, close endpoints, /system/status'"
```

---

## Task 3: Multi-timeframe Analysis

**Files:**
- Modify: `~/ibkr-bot/app/scanner/preprocessor.py`
- Create: `~/ibkr-bot/tests/test_multitimeframe.py`

- [ ] **Step 1: Escribir tests del clasificador multi-timeframe**

```python
# tests/test_multitimeframe.py
from app.scanner.preprocessor import classify_signal, classify_multitimeframe


def test_classify_single_strong():
    assert classify_signal(rsi=28.0, macd_crossover=True, volume_ratio=1.6) == "STRONG"


def test_classify_single_weak():
    assert classify_signal(rsi=50.0, macd_crossover=False, volume_ratio=1.0) == "WEAK"


def test_multitimeframe_strong_when_all_confirm():
    # daily STRONG, hourly STRONG, minute MEDIUM -> STRONG
    result = classify_multitimeframe("STRONG", "STRONG", "MEDIUM")
    assert result == "STRONG"


def test_multitimeframe_medium_when_two_confirm():
    # daily STRONG, hourly MEDIUM, minute WEAK -> MEDIUM
    result = classify_multitimeframe("STRONG", "MEDIUM", "WEAK")
    assert result == "MEDIUM"


def test_multitimeframe_weak_when_only_one():
    # daily STRONG, rest WEAK -> WEAK (no signal to LLM)
    result = classify_multitimeframe("STRONG", "WEAK", "WEAK")
    assert result == "WEAK"


def test_multitimeframe_weak_when_all_weak():
    result = classify_multitimeframe("WEAK", "WEAK", "WEAK")
    assert result == "WEAK"


def test_multitimeframe_strong_requires_daily_confirmation():
    # minute STRONG + hourly STRONG but daily WEAK -> MEDIUM (daily is anchor)
    result = classify_multitimeframe("WEAK", "STRONG", "STRONG")
    assert result == "MEDIUM"
```

- [ ] **Step 2: Correr tests — verificar que fallan**

```bash
ssh aiutox-pi "cd ~/ibkr-bot && source .venv/bin/activate && pytest tests/test_multitimeframe.py -v 2>&1 | tail -8"
```
Esperado: `ImportError: cannot import name 'classify_multitimeframe'`

- [ ] **Step 3: Actualizar preprocessor.py con multi-timeframe**

Reemplazar `~/ibkr-bot/app/scanner/preprocessor.py` completo:

```python
# app/scanner/preprocessor.py
"""
Preprocesador de señales técnicas multi-timeframe.
Corre cada 15 min en horario de mercado via APScheduler.
Señal solo cuando daily + al menos hourly o 5min confirman.
Nunca llama al LLM — solo calcula e inserta señales.
"""
import logging
from datetime import datetime

import pandas as pd

from app.config.settings import (
    MARKET_TZ, MARKET_OPEN_HOUR, MARKET_OPEN_MINUTE,
    MARKET_CLOSE_HOUR, MARKET_CLOSE_MINUTE,
)
from app.db.database import get_approved_symbols, insert_signal
from app.db.models import Signal

logger = logging.getLogger(__name__)


def _calc_indicators(df: pd.DataFrame) -> dict:
    """Calcula RSI, MACD crossover y volume ratio de un DataFrame OHLCV."""
    if len(df) < 15:
        return {}
    delta = df["close"].diff()
    gain = delta.clip(lower=0).rolling(14).mean()
    loss = (-delta.clip(upper=0)).rolling(14).mean()
    rs = gain / loss
    rsi = float((100 - 100 / (1 + rs)).iloc[-1])

    ema12 = df["close"].ewm(span=12).mean()
    ema26 = df["close"].ewm(span=26).mean()
    macd_line = ema12 - ema26
    signal_line = macd_line.ewm(span=9).mean()
    macd_crossover = (
        (macd_line.iloc[-2] < signal_line.iloc[-2] and macd_line.iloc[-1] > signal_line.iloc[-1]) or
        (macd_line.iloc[-2] > signal_line.iloc[-2] and macd_line.iloc[-1] < signal_line.iloc[-1])
    )

    avg_vol = df["volume"].rolling(20).mean().iloc[-1]
    volume_ratio = float(df["volume"].iloc[-1] / avg_vol) if avg_vol > 0 else 1.0

    return {"rsi": round(rsi, 2), "macd": round(float(macd_line.iloc[-1]), 4),
            "macd_crossover": macd_crossover, "volume_ratio": round(volume_ratio, 2)}


def classify_signal(rsi: float, macd_crossover: bool, volume_ratio: float) -> str:
    """Clasifica señal de un solo timeframe."""
    conditions = [rsi < 30 or rsi > 70, macd_crossover, volume_ratio > 1.5]
    count = sum(conditions)
    if count == 3:
        return "STRONG"
    if count == 2:
        return "MEDIUM"
    return "WEAK"


def classify_multitimeframe(daily: str, hourly: str, minute: str) -> str:
    """
    Clasifica señal combinada de 3 timeframes.
    Daily es el anchor — sin señal diaria no hay STRONG.
    STRONG: daily confirma + al menos uno más confirma (STRONG o MEDIUM)
    MEDIUM: daily confirma O dos timeframes menores confirman
    WEAK: todo lo demás
    """
    strength_val = {"STRONG": 2, "MEDIUM": 1, "WEAK": 0}
    d, h, m = strength_val[daily], strength_val[hourly], strength_val[minute]

    # STRONG: daily >= MEDIUM y al menos uno de los menores >= MEDIUM
    if d >= 1 and (h >= 1 or m >= 1) and (d + h + m) >= 3:
        return "STRONG"
    # MEDIUM: daily >= MEDIUM O dos menores confirman
    if d >= 1 or (h >= 1 and m >= 1):
        return "MEDIUM"
    return "WEAK"


def _fetch_bars(ib_client, contract, duration: str, bar_size: str) -> pd.DataFrame:
    """Descarga barras históricas de IB y retorna DataFrame."""
    bars = ib_client.ib.reqHistoricalData(
        contract, endDateTime="", durationStr=duration,
        barSizeSetting=bar_size, whatToShow="TRADES",
        useRTH=True, formatDate=1,
    )
    if not bars or len(bars) < 15:
        return pd.DataFrame()
    return pd.DataFrame([{"close": b.close, "volume": b.volume} for b in bars])


def _is_market_hours(now: datetime) -> bool:
    et = now.astimezone(MARKET_TZ)
    if et.weekday() >= 5:
        return False
    open_t = et.replace(hour=MARKET_OPEN_HOUR, minute=MARKET_OPEN_MINUTE, second=0)
    close_t = et.replace(hour=MARKET_CLOSE_HOUR, minute=MARKET_CLOSE_MINUTE, second=0)
    return open_t <= et <= close_t


def scan_symbol(symbol: str, ib_client) -> str | None:
    """
    Escanea un símbolo en 3 timeframes y retorna la señal combinada.
    Solo inserta en DB si la señal es STRONG o MEDIUM.
    """
    try:
        from ib_insync import Stock
        contract = Stock(symbol, "SMART", "USD")

        # Daily — 30 días
        df_daily = _fetch_bars(ib_client, contract, "30 D", "1 day")
        ind_daily = _calc_indicators(df_daily)
        if not ind_daily:
            logger.warning(f"Not enough daily bars for {symbol}")
            return None
        sig_daily = classify_signal(ind_daily["rsi"], ind_daily["macd_crossover"], ind_daily["volume_ratio"])

        # Hourly — 5 días
        df_hourly = _fetch_bars(ib_client, contract, "5 D", "1 hour")
        ind_hourly = _calc_indicators(df_hourly)
        sig_hourly = classify_signal(
            ind_hourly["rsi"], ind_hourly["macd_crossover"], ind_hourly["volume_ratio"]
        ) if ind_hourly else "WEAK"

        # 5 minutos — 1 día
        df_5min = _fetch_bars(ib_client, contract, "1 D", "5 mins")
        ind_5min = _calc_indicators(df_5min)
        sig_5min = classify_signal(
            ind_5min["rsi"], ind_5min["macd_crossover"], ind_5min["volume_ratio"]
        ) if ind_5min else "WEAK"

        strength = classify_multitimeframe(sig_daily, sig_hourly, sig_5min)

        logger.debug(
            f"{symbol}: daily={sig_daily} hourly={sig_hourly} 5min={sig_5min} -> {strength}"
        )

        if strength in ("STRONG", "MEDIUM"):
            insert_signal(Signal(
                id=None, symbol=symbol, strength=strength,
                rsi=ind_daily["rsi"], macd=ind_daily["macd"],
                volume_ratio=ind_daily["volume_ratio"],
                extra_indicators=f'{{"daily":"{sig_daily}","hourly":"{sig_hourly}","5min":"{sig_5min}"}}',
                created_at=datetime.now(tz=MARKET_TZ),
            ))
            logger.info(
                f"Signal {strength} for {symbol} "
                f"[D:{sig_daily} H:{sig_hourly} 5m:{sig_5min}] "
                f"RSI:{ind_daily['rsi']} Vol:{ind_daily['volume_ratio']}"
            )
            return strength

        return None

    except Exception as e:
        logger.error(f"Error scanning {symbol}: {e}")
        return None


def run_scan(ib_client):
    """Punto de entrada del scheduler."""
    now = datetime.now(tz=MARKET_TZ)
    if not _is_market_hours(now):
        logger.debug("Outside market hours — skipping scan")
        return
    symbols = get_approved_symbols()
    logger.info(f"Multi-timeframe scan: {len(symbols)} symbols")
    for symbol in symbols:
        scan_symbol(symbol, ib_client)
```

- [ ] **Step 4: Correr tests**

```bash
ssh aiutox-pi "cd ~/ibkr-bot && source .venv/bin/activate && pytest tests/test_multitimeframe.py -v 2>&1"
```
Esperado: 7 PASS

- [ ] **Step 5: Commit**

```bash
ssh aiutox-pi "cd ~/ibkr-bot && git add app/scanner/preprocessor.py tests/test_multitimeframe.py && git commit -m 'feat: multi-timeframe analysis daily+hourly+5min with confluence signal'"
```

---

## Task 4: News/Sentiment via RSS

**Files:**
- Create: `~/ibkr-bot/app/scanner/news.py`
- Create: `~/ibkr-bot/tests/test_news.py`

- [ ] **Step 1: Instalar feedparser**

```bash
ssh aiutox-pi "cd ~/ibkr-bot && source .venv/bin/activate && pip install feedparser && python3 -c 'import feedparser; print(feedparser.__version__)'"
```
Esperado: versión impresa sin error.

- [ ] **Step 2: Escribir tests**

```python
# tests/test_news.py
from unittest.mock import patch, MagicMock
from app.scanner.news import get_news_summary, _extract_sentiment


def test_extract_sentiment_positive():
    text = "AAPL stock surges to record high after strong earnings beat"
    sentiment = _extract_sentiment(text)
    assert sentiment == "positive"


def test_extract_sentiment_negative():
    text = "AAPL crashes amid recession fears and disappointing revenue"
    sentiment = _extract_sentiment(text)
    assert sentiment == "negative"


def test_extract_sentiment_neutral():
    text = "AAPL announces new product line scheduled for next quarter"
    sentiment = _extract_sentiment(text)
    assert sentiment == "neutral"


def test_get_news_summary_returns_string():
    with patch("app.scanner.news.feedparser") as mock_fp:
        mock_fp.parse.return_value = MagicMock(entries=[
            MagicMock(title="AAPL beats earnings", summary="Apple reported strong Q3 results"),
            MagicMock(title="Market rallies on Fed news", summary="Stocks rise after Fed decision"),
        ])
        result = get_news_summary("AAPL")
        assert isinstance(result, str)
        assert len(result) > 0


def test_get_news_summary_handles_no_news():
    with patch("app.scanner.news.feedparser") as mock_fp:
        mock_fp.parse.return_value = MagicMock(entries=[])
        result = get_news_summary("FAKE")
        assert "No recent news" in result
```

- [ ] **Step 3: Correr tests — verificar que fallan**

```bash
ssh aiutox-pi "cd ~/ibkr-bot && source .venv/bin/activate && pytest tests/test_news.py -v 2>&1 | tail -5"
```
Esperado: `ModuleNotFoundError`

- [ ] **Step 4: Crear app/scanner/news.py**

```python
# app/scanner/news.py
"""
Obtiene noticias recientes de Yahoo Finance RSS para un símbolo.
Proporciona contexto de sentiment al LLM antes de decidir.
"""
import logging
import feedparser

logger = logging.getLogger(__name__)

POSITIVE_WORDS = {
    "surge", "rally", "beat", "record", "high", "gain", "rise", "up",
    "strong", "growth", "profit", "bullish", "upgrade", "buy", "positive",
    "exceeds", "outperform", "boost", "jump", "soar", "breakout",
}
NEGATIVE_WORDS = {
    "crash", "fall", "drop", "loss", "miss", "low", "down", "weak",
    "decline", "recession", "fear", "risk", "cut", "downgrade", "sell",
    "bearish", "warning", "disappoint", "plunge", "slump", "layoff",
}


def _extract_sentiment(text: str) -> str:
    """Clasifica el sentimiento de un texto como positive/negative/neutral."""
    words = text.lower().split()
    pos = sum(1 for w in words if w.strip(".,!?") in POSITIVE_WORDS)
    neg = sum(1 for w in words if w.strip(".,!?") in NEGATIVE_WORDS)
    if pos > neg:
        return "positive"
    if neg > pos:
        return "negative"
    return "neutral"


def get_news_summary(symbol: str, max_items: int = 3) -> str:
    """
    Descarga las últimas noticias de Yahoo Finance RSS para un símbolo.
    Retorna un resumen en texto para incluir en el prompt del LLM.
    """
    url = f"https://feeds.finance.yahoo.com/rss/2.0/headline?s={symbol}&region=US&lang=en-US"
    try:
        feed = feedparser.parse(url)
        entries = feed.entries[:max_items]

        if not entries:
            return f"No recent news found for {symbol}."

        lines = []
        for entry in entries:
            title = getattr(entry, "title", "")
            summary = getattr(entry, "summary", "")
            text = f"{title} {summary}"
            sentiment = _extract_sentiment(text)
            lines.append(f"[{sentiment.upper()}] {title}")

        return "\n".join(lines)

    except Exception as e:
        logger.error(f"Could not fetch news for {symbol}: {e}")
        return f"News unavailable for {symbol}."
```

- [ ] **Step 5: Correr tests**

```bash
ssh aiutox-pi "cd ~/ibkr-bot && source .venv/bin/activate && pytest tests/test_news.py -v 2>&1"
```
Esperado: 4 PASS

- [ ] **Step 6: Integrar news en agent.py**

Leer agent.py actual:
```bash
ssh aiutox-pi "grep -n 'def _get_context\|news\|patterns_text' ~/ibkr-bot/app/llm/agent.py"
```

Modificar `_get_context()` en `app/llm/agent.py` para incluir noticias:
```python
def _get_context(symbol: str) -> dict:
    """Obtiene precio, portafolio, cuenta, patrones y noticias via FastAPI."""
    from app.scanner.news import get_news_summary
    ctx = {}
    try:
        ctx["price"] = httpx.get(f"{API_BASE}/price/{symbol}", timeout=15).json()
    except Exception:
        ctx["price"] = {}
    try:
        ctx["portfolio"] = httpx.get(f"{API_BASE}/portfolio", timeout=15).json()
    except Exception:
        ctx["portfolio"] = []
    try:
        ctx["account"] = httpx.get(f"{API_BASE}/account", timeout=15).json()
    except Exception:
        ctx["account"] = {}
    try:
        ctx["patterns"] = [
            {"pattern": p.pattern_text, "wins": p.win_count, "losses": p.loss_count}
            for p in get_patterns_for_symbol(symbol)
        ]
    except Exception:
        ctx["patterns"] = []
    ctx["news"] = get_news_summary(symbol)
    return ctx
```

Y agregar las noticias al prompt en `analyze_signal()` — reemplazar la sección `PATRONES` con:
```python
    news_text = ctx.get("news", "Sin noticias disponibles.")

    prompt = f"""Eres un agente de swing trading. Analiza esta señal y decide si entrar en una posicion.

SEÑAL TECNICA:
- Simbolo: {symbol}
- Fuerza: {strength} (daily+hourly+5min)
- RSI: {rsi}
- MACD: {macd}
- Volumen ratio: {volume_ratio}x

CONTEXTO DE MERCADO:
- Precio actual: ${price:.2f}
- Posiciones activas: {active_positions}/3
- Capital disponible: ${capital:.2f}

NOTICIAS RECIENTES:
{news_text}

PATRONES APRENDIDOS PARA {symbol}:
{patterns_text}

REGLAS OBLIGATORIAS:
- take_profit_pct debe ser al menos 2x el stop_loss_pct
- Si hay noticias negativas importantes, sé mas conservador
- Si no hay evidencia suficiente, responde IGNORE

Responde UNICAMENTE con este JSON:
{{"action":"BUY","stop_loss_pct":0.025,"take_profit_pct":0.06,"justification":"razon concisa","confidence":"HIGH"}}"""
```

- [ ] **Step 7: Commit**

```bash
ssh aiutox-pi "cd ~/ibkr-bot && git add app/scanner/news.py app/llm/agent.py tests/test_news.py && git commit -m 'feat: add RSS news sentiment as LLM context'"
```

---

## Task 5: Telegram Bot Bidireccional

**Files:**
- Create: `~/ibkr-bot/app/notifications/telegram_bot.py`
- Modify: `~/ibkr-bot/run.py`

- [ ] **Step 1: Crear app/notifications/telegram_bot.py**

```python
# app/notifications/telegram_bot.py
"""
Telegram bot bidireccional para el IBKR AI Trader.

Comandos directos (sin LLM) → FastAPI:
  /estado, /posiciones, /historial, /señales, /simbolos
  /pausar, /reanudar, /stop
  /cerrar SYMBOL, /cerrar todo
  /modo paper, /modo live
  /aprobar SYMBOL, /ayuda

Comandos con LLM (OpenCode + MCP):
  /analizar SYMBOL
  /proponer SYMBOL razón
  texto libre en español
"""
import asyncio
import json
import logging
import subprocess
import threading
import httpx

from telegram import Update, Bot
from telegram.ext import (
    Application, CommandHandler, MessageHandler,
    ContextTypes, filters,
)

from app.config.settings import TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID
from app.notifications.telegram import notify

logger = logging.getLogger(__name__)

API_BASE = "http://127.0.0.1:8088"
OPENCODE_BIN = "/home/frankpach/.opencode/bin/opencode"
OPENCODE_MODEL = "opencode-go/qwen3.5-plus"

_scheduler = None  # inyectado desde run.py


def _api(method: str, path: str, **kwargs) -> dict:
    """Llamada HTTP a FastAPI local."""
    try:
        fn = getattr(httpx, method)
        r = fn(f"{API_BASE}{path}", timeout=30, **kwargs)
        return r.json()
    except Exception as e:
        return {"error": str(e)}


def _call_opencode_with_mcp(prompt: str) -> str:
    """Llama a OpenCode con MCP tools disponibles y retorna la respuesta en texto."""
    try:
        result = subprocess.run(
            [OPENCODE_BIN, "run", "--model", OPENCODE_MODEL, "--format", "json", prompt],
            capture_output=True, text=True, timeout=90,
            cwd="/home/frankpach/ibkr-bot",
        )
        text_parts = []
        for line in result.stdout.strip().splitlines():
            if not line.strip():
                continue
            try:
                event = json.loads(line)
                if event.get("type") == "text":
                    text_parts.append(event["part"]["text"])
            except json.JSONDecodeError:
                continue
        return "".join(text_parts).strip() or "Sin respuesta del LLM."
    except subprocess.TimeoutExpired:
        return "El LLM tardó demasiado. Intenta de nuevo."
    except Exception as e:
        return f"Error al llamar al LLM: {e}"


def _only_owner(func):
    """Decorator: solo responde al chat_id configurado."""
    async def wrapper(update: Update, ctx: ContextTypes.DEFAULT_TYPE):
        if str(update.effective_chat.id) != str(TELEGRAM_CHAT_ID):
            return
        return await func(update, ctx)
    return wrapper


# --- Comandos directos (sin LLM) ---

@_only_owner
async def cmd_estado(update: Update, ctx: ContextTypes.DEFAULT_TYPE):
    data = _api("get", "/system/status")
    portfolio = _api("get", "/portfolio")
    if isinstance(portfolio, list):
        pos_text = "\n".join(
            f"  • {p['symbol']}: {p['quantity']} acc @ ${p['avg_cost']:.2f} | P&L: ${p['unrealized_pnl']:.2f}"
            for p in portfolio
        ) or "  Sin posiciones abiertas"
    else:
        pos_text = "  Error al obtener posiciones"
    msg = (
        f"Estado del sistema\n\n"
        f"Modo: {data.get('mode', '?').upper()}\n"
        f"Pausado: {'Si' if data.get('paused') else 'No'}\n"
        f"Capital simulado: ${data.get('simulated_capital', 500)}\n"
        f"P&L hoy: ${data.get('daily_pnl_usd', 0):.2f} ({data.get('daily_pnl_pct', 0):.2f}%)\n"
        f"Posiciones abiertas: {data.get('open_positions', 0)}/3\n\n"
        f"Posiciones:\n{pos_text}"
    )
    await update.message.reply_text(msg)


@_only_owner
async def cmd_posiciones(update: Update, ctx: ContextTypes.DEFAULT_TYPE):
    trades = _api("get", "/trades")
    if not trades or not isinstance(trades, list):
        await update.message.reply_text("Sin posiciones abiertas.")
        return
    lines = []
    for t in trades:
        lines.append(
            f"{t['symbol']} {t['action']} x{t['quantity']}\n"
            f"  Entrada: ${t['entry_price']:.2f}\n"
            f"  SL: ${t['stop_loss_price']:.2f} | TP: ${t['take_profit_price']:.2f}\n"
            f"  Señal: {t['signal_strength']}"
        )
    await update.message.reply_text("Posiciones abiertas:\n\n" + "\n\n".join(lines))


@_only_owner
async def cmd_historial(update: Update, ctx: ContextTypes.DEFAULT_TYPE):
    trades = _api("get", "/trades/closed?limit=5")
    if not trades or not isinstance(trades, list):
        await update.message.reply_text("Sin historial de operaciones.")
        return
    lines = []
    for t in trades:
        pnl = t.get("pnl_usd", 0) or 0
        emoji = "+" if pnl >= 0 else "-"
        lines.append(
            f"{emoji} {t['symbol']} {t['action']} | ${pnl:.2f} ({t.get('exit_reason','?')})"
        )
    await update.message.reply_text("Ultimas operaciones:\n" + "\n".join(lines))


@_only_owner
async def cmd_senales(update: Update, ctx: ContextTypes.DEFAULT_TYPE):
    signals = _api("get", "/signals")
    if not signals or not isinstance(signals, list):
        await update.message.reply_text("Sin señales pendientes.")
        return
    lines = [f"{s['symbol']} [{s['strength']}] RSI:{s['rsi']} Vol:{s['volume_ratio']}x" for s in signals]
    await update.message.reply_text("Señales pendientes:\n" + "\n".join(lines))


@_only_owner
async def cmd_simbolos(update: Update, ctx: ContextTypes.DEFAULT_TYPE):
    data = _api("get", "/allowed-symbols")
    symbols = data.get("symbols", [])
    await update.message.reply_text("Universo activo:\n" + ", ".join(symbols))


@_only_owner
async def cmd_pausar(update: Update, ctx: ContextTypes.DEFAULT_TYPE):
    _api("post", "/system/pause")
    await update.message.reply_text("Sistema pausado. El scanner no correra hasta /reanudar")


@_only_owner
async def cmd_reanudar(update: Update, ctx: ContextTypes.DEFAULT_TYPE):
    _api("post", "/system/resume")
    await update.message.reply_text("Sistema reanudado.")


@_only_owner
async def cmd_stop(update: Update, ctx: ContextTypes.DEFAULT_TYPE):
    await update.message.reply_text("Deteniendo el sistema...")
    notify("Sistema detenido por comando /stop desde Telegram.")
    import os, signal
    os.kill(os.getpid(), signal.SIGTERM)


@_only_owner
async def cmd_cerrar(update: Update, ctx: ContextTypes.DEFAULT_TYPE):
    args = ctx.args
    if not args:
        await update.message.reply_text("Uso: /cerrar SYMBOL o /cerrar todo")
        return
    if args[0].lower() == "todo":
        data = _api("post", "/orders/close-all")
        closed = data.get("closed", 0)
        await update.message.reply_text(f"Cerradas {closed} posiciones.")
    else:
        symbol = args[0].upper()
        data = _api("post", f"/orders/close/{symbol}")
        if "error" in data:
            await update.message.reply_text(f"Error: {data['error']}")
        else:
            pnl = data.get("pnl_usd", 0)
            await update.message.reply_text(
                f"{symbol} cerrada @ ${data.get('exit_price', '?'):.2f}\n"
                f"P&L: ${pnl:.2f} ({data.get('pnl_pct', 0):.2f}%)"
            )


@_only_owner
async def cmd_modo(update: Update, ctx: ContextTypes.DEFAULT_TYPE):
    args = ctx.args
    if not args or args[0].lower() not in ("paper", "live"):
        await update.message.reply_text("Uso: /modo paper o /modo live")
        return
    mode = args[0].lower()
    if mode == "live":
        await update.message.reply_text(
            "ADVERTENCIA: Cambiar a modo LIVE ejecutara ordenes reales.\n"
            "Confirma escribiendo: /modo live confirmar"
        )
        return
    _api("post", f"/system/mode/{mode}")
    await update.message.reply_text(f"Modo cambiado a {mode.upper()}.")


@_only_owner
async def cmd_aprobar(update: Update, ctx: ContextTypes.DEFAULT_TYPE):
    if not ctx.args:
        await update.message.reply_text("Uso: /aprobar SYMBOL")
        return
    symbol = ctx.args[0].upper()
    data = _api("post", f"/symbols/approve/{symbol}")
    await update.message.reply_text(f"{symbol}: {data.get('message', 'Aprobado.')}")


@_only_owner
async def cmd_ayuda(update: Update, ctx: ContextTypes.DEFAULT_TYPE):
    await update.message.reply_text(
        "Comandos disponibles:\n\n"
        "Informacion:\n"
        "  /estado — resumen del sistema\n"
        "  /posiciones — posiciones abiertas\n"
        "  /historial — ultimas 5 operaciones\n"
        "  /senales — señales pendientes\n"
        "  /simbolos — universo de trading\n\n"
        "Control:\n"
        "  /pausar — detener el scanner\n"
        "  /reanudar — reactivar el scanner\n"
        "  /cerrar SYMBOL — cerrar posicion\n"
        "  /cerrar todo — cerrar todo\n"
        "  /modo paper|live — cambiar modo\n"
        "  /aprobar SYMBOL — aprobar simbolo\n"
        "  /stop — detener el sistema\n\n"
        "Analisis (usa LLM):\n"
        "  /analizar SYMBOL — analisis completo\n"
        "  /proponer SYMBOL razon — proponer simbolo\n"
        "  Cualquier mensaje de texto — consulta libre"
    )


# --- Comandos con LLM ---

@_only_owner
async def cmd_analizar(update: Update, ctx: ContextTypes.DEFAULT_TYPE):
    if not ctx.args:
        await update.message.reply_text("Uso: /analizar SYMBOL (ej: /analizar AAPL)")
        return
    symbol = ctx.args[0].upper()
    await update.message.reply_text(f"Analizando {symbol}...")

    prompt = (
        f"Analiza el simbolo {symbol} para trading. "
        f"Usa las herramientas disponibles para obtener el precio actual, "
        f"las posiciones abiertas, el balance de la cuenta, y los patrones aprendidos. "
        f"Luego determina si hay oportunidad de compra o venta. "
        f"Si ves una oportunidad clara, usa preview_order para mostrar los detalles. "
        f"Responde en español de forma concisa con tu analisis y recomendacion. "
        f"Si recomiendas operar, indica claramente los parametros (SL%, TP%) y pregunta si ejecuto."
    )

    response = await asyncio.get_event_loop().run_in_executor(
        None, _call_opencode_with_mcp, prompt
    )
    await update.message.reply_text(response[:4000])


@_only_owner
async def cmd_proponer(update: Update, ctx: ContextTypes.DEFAULT_TYPE):
    if len(ctx.args) < 2:
        await update.message.reply_text("Uso: /proponer SYMBOL razon")
        return
    symbol = ctx.args[0].upper()
    reason = " ".join(ctx.args[1:])
    data = _api("post", "/symbols/propose", json={"symbol": symbol, "reason": reason})
    await update.message.reply_text(data.get("message", f"{symbol} propuesto."))


@_only_owner
async def handle_message(update: Update, ctx: ContextTypes.DEFAULT_TYPE):
    """Maneja mensajes de texto libre — los pasa al LLM con contexto del sistema."""
    text = update.message.text
    await update.message.reply_text("Consultando al LLM...")

    prompt = (
        f"Eres el asistente del sistema IBKR AI Trader. El usuario te dice: '{text}'\n\n"
        f"Tienes acceso a herramientas para: ver precios, portafolio, cuenta, señales, "
        f"patrones aprendidos, simular ordenes y ejecutar ordenes. "
        f"Usa las herramientas que necesites segun la solicitud. "
        f"Responde en español de forma clara y concisa. "
        f"Si el usuario pide ejecutar una operacion, primero muestra preview_order y confirma."
    )

    response = await asyncio.get_event_loop().run_in_executor(
        None, _call_opencode_with_mcp, prompt
    )
    await update.message.reply_text(response[:4000])


def start_bot(scheduler):
    """Arranca el bot en un nuevo event loop (corre en thread separado)."""
    global _scheduler
    _scheduler = scheduler

    if not TELEGRAM_BOT_TOKEN:
        logger.warning("TELEGRAM_BOT_TOKEN not set — bot not started")
        return

    async def run():
        app = Application.builder().token(TELEGRAM_BOT_TOKEN).build()
        app.add_handler(CommandHandler("estado", cmd_estado))
        app.add_handler(CommandHandler("posiciones", cmd_posiciones))
        app.add_handler(CommandHandler("historial", cmd_historial))
        app.add_handler(CommandHandler("senales", cmd_senales))
        app.add_handler(CommandHandler("simbolos", cmd_simbolos))
        app.add_handler(CommandHandler("pausar", cmd_pausar))
        app.add_handler(CommandHandler("reanudar", cmd_reanudar))
        app.add_handler(CommandHandler("stop", cmd_stop))
        app.add_handler(CommandHandler("cerrar", cmd_cerrar))
        app.add_handler(CommandHandler("modo", cmd_modo))
        app.add_handler(CommandHandler("aprobar", cmd_aprobar))
        app.add_handler(CommandHandler("ayuda", cmd_ayuda))
        app.add_handler(CommandHandler("analizar", cmd_analizar))
        app.add_handler(CommandHandler("proponer", cmd_proponer))
        app.add_handler(MessageHandler(filters.TEXT & ~filters.COMMAND, handle_message))
        logger.info("Telegram bot started")
        await app.initialize()
        await app.start()
        await app.updater.start_polling(drop_pending_updates=True)
        # Corre indefinidamente
        await asyncio.Event().wait()

    loop = asyncio.new_event_loop()
    asyncio.set_event_loop(loop)
    loop.run_until_complete(run())
```

- [ ] **Step 2: Actualizar run.py para iniciar bot + reconciliación + circuit breaker**

Reemplazar `~/ibkr-bot/run.py` completo:

```python
# run.py
import logging
import threading
import uvicorn
from apscheduler.schedulers.background import BackgroundScheduler

from app.db.database import init_db, get_daily_pnl
from app.ibkr.client import IBKRClient
from app.scanner.preprocessor import run_scan
from app.positions.manager import check_positions
from app.llm.loop import process_pending_signals
from app.system.controller import init_controller
from app.system.reconciler import reconcile_positions
from app.notifications.telegram import notify
from app.notifications.telegram_bot import start_bot
from app.config.settings import SCAN_INTERVAL_MINUTES, POSITION_CHECK_MINUTES, SIMULATED_CAPITAL

logging.basicConfig(level=logging.INFO, format="%(asctime)s %(levelname)s %(name)s - %(message)s")
logger = logging.getLogger(__name__)


def main():
    logger.info("Initializing database...")
    init_db()

    logger.info("Connecting to IB Gateway...")
    ib_client = IBKRClient()

    # Reconciliar posiciones al arrancar
    logger.info("Reconciling positions...")
    reconcile_positions(ib_client)

    # Inicializar scheduler
    scheduler = BackgroundScheduler()

    # Inicializar system controller
    ctrl = init_controller(scheduler)

    def _check_circuit_breaker():
        daily_pnl = get_daily_pnl()
        ctrl.check_circuit_breaker(daily_pnl, SIMULATED_CAPITAL)

    scheduler.add_job(lambda: run_scan(ib_client), "interval", minutes=SCAN_INTERVAL_MINUTES, id="scanner")
    scheduler.add_job(lambda: check_positions(), "interval", minutes=POSITION_CHECK_MINUTES, id="position_manager")
    scheduler.add_job(process_pending_signals, "interval", minutes=SCAN_INTERVAL_MINUTES, id="signal_processor")
    scheduler.add_job(_check_circuit_breaker, "interval", minutes=POSITION_CHECK_MINUTES, id="circuit_breaker")
    scheduler.start()

    logger.info(f"Scheduler started — scan every {SCAN_INTERVAL_MINUTES}min, positions every {POSITION_CHECK_MINUTES}min")

    # Arrancar Telegram bot en thread separado
    bot_thread = threading.Thread(target=start_bot, args=(scheduler,), daemon=True)
    bot_thread.start()
    logger.info("Telegram bot thread started")

    notify(
        "IBKR AI Trader v2 iniciado\n"
        f"Scanner: cada {SCAN_INTERVAL_MINUTES} min (multi-timeframe)\n"
        f"Capital simulado: ${SIMULATED_CAPITAL}\n"
        f"Circuit breaker: 5% perdida diaria\n"
        f"Modo: Paper Trading\n"
        f"Escribe /ayuda para ver comandos"
    )

    logger.info("Starting FastAPI on 127.0.0.1:8088...")
    uvicorn.run("app.api.main:app", host="127.0.0.1", port=8088, workers=1)


if __name__ == "__main__":
    main()
```

- [ ] **Step 3: Reiniciar sistema y verificar bot**

```bash
ssh aiutox-pi "kill \$(ps aux | grep 'run.py' | grep -v grep | awk '{print \$2}') 2>/dev/null; sleep 2"
ssh aiutox-pi "cd ~/ibkr-bot && source .venv/bin/activate && nohup python3 run.py > /tmp/run.log 2>&1 & sleep 12 && curl -s http://127.0.0.1:8088/health && echo '' && tail -15 /tmp/run.log"
```
Esperado: health OK, logs muestran "Telegram bot thread started".

Deberías recibir en Telegram el mensaje de inicio con los comandos disponibles.

- [ ] **Step 4: Probar /ayuda desde Telegram**

Envía `/ayuda` al bot y verifica que responde con la lista de comandos.

- [ ] **Step 5: Probar /estado desde Telegram**

Envía `/estado` al bot y verifica que responde con el estado del sistema.

- [ ] **Step 6: Commit**

```bash
ssh aiutox-pi "cd ~/ibkr-bot && git add app/notifications/telegram_bot.py run.py && git commit -m 'feat: bidirectional Telegram bot with direct commands and LLM handler'"
```

---

## Task 6: Tests del bot + verificación final

**Files:**
- Create: `~/ibkr-bot/tests/test_telegram_bot.py`

- [ ] **Step 1: Crear tests del bot**

```python
# tests/test_telegram_bot.py
from unittest.mock import AsyncMock, MagicMock, patch
import pytest


def make_update(text="/estado", chat_id="8645527459"):
    update = MagicMock()
    update.effective_chat.id = chat_id
    update.message.text = text
    update.message.reply_text = AsyncMock()
    return update


@pytest.mark.asyncio
async def test_estado_returns_system_info():
    with patch("app.notifications.telegram_bot._api") as mock_api:
        mock_api.side_effect = [
            {"mode": "paper", "paused": False, "simulated_capital": 500,
             "daily_pnl_usd": 0.0, "daily_pnl_pct": 0.0, "open_positions": 0},
            [],
        ]
        from app.notifications.telegram_bot import cmd_estado
        update = make_update("/estado")
        await cmd_estado(update, MagicMock())
        update.message.reply_text.assert_called_once()
        call_text = update.message.reply_text.call_args[0][0]
        assert "paper" in call_text.lower()


@pytest.mark.asyncio
async def test_cerrar_sin_args_muestra_ayuda():
    from app.notifications.telegram_bot import cmd_cerrar
    update = make_update("/cerrar")
    ctx = MagicMock()
    ctx.args = []
    await cmd_cerrar(update, ctx)
    call_text = update.message.reply_text.call_args[0][0]
    assert "Uso" in call_text


@pytest.mark.asyncio
async def test_modo_live_pide_confirmacion():
    from app.notifications.telegram_bot import cmd_modo
    update = make_update("/modo live")
    ctx = MagicMock()
    ctx.args = ["live"]
    await cmd_modo(update, ctx)
    call_text = update.message.reply_text.call_args[0][0]
    assert "ADVERTENCIA" in call_text or "confirmar" in call_text.lower()


@pytest.mark.asyncio
async def test_owner_filter_blocks_other_chats():
    with patch("app.notifications.telegram_bot.TELEGRAM_CHAT_ID", "8645527459"):
        from app.notifications.telegram_bot import cmd_estado
        update = make_update(chat_id="999999")
        await cmd_estado(update, MagicMock())
        update.message.reply_text.assert_not_called()
```

- [ ] **Step 2: Instalar pytest-asyncio**

```bash
ssh aiutox-pi "cd ~/ibkr-bot && source .venv/bin/activate && pip install pytest-asyncio && python3 -c 'import pytest_asyncio; print(\"OK\")'"
```

- [ ] **Step 3: Agregar asyncio mode a pytest.ini**

```bash
ssh aiutox-pi "echo 'asyncio_mode = auto' >> ~/ibkr-bot/pytest.ini && cat ~/ibkr-bot/pytest.ini"
```

- [ ] **Step 4: Correr todos los tests**

```bash
ssh aiutox-pi "cd ~/ibkr-bot && source .venv/bin/activate && pytest tests/test_telegram_bot.py tests/test_system_controller.py tests/test_multitimeframe.py tests/test_news.py tests/test_risk_validator.py tests/test_preprocessor.py tests/test_signal_loop.py tests/test_postmortem.py -v 2>&1 | tail -20"
```
Esperado: mínimo 30 PASS

- [ ] **Step 5: Commit final**

```bash
ssh aiutox-pi "cd ~/ibkr-bot && git add tests/test_telegram_bot.py pytest.ini && git commit -m 'test: add telegram bot tests, finalize Plan 1'"
```

---

## Self-Review

**Cobertura:**
- ✅ Cerrar posición AAPL de prueba — Task 1
- ✅ Reconciliación DB vs IB al arranque — Task 1 (reconciler.py) + run.py
- ✅ get_closed_trades, get_daily_pnl — Task 1
- ✅ System controller pause/resume/mode — Task 2
- ✅ Circuit breaker diario 5% — Task 2 + run.py
- ✅ Endpoints /system/status, /system/pause, /system/resume, /system/mode — Task 2
- ✅ Endpoints /orders/close/{symbol}, /orders/close-all, /trades/closed — Task 2
- ✅ Multi-timeframe daily+hourly+5min — Task 3
- ✅ classify_multitimeframe con daily como anchor — Task 3
- ✅ News/sentiment RSS Yahoo Finance — Task 4
- ✅ News integrado en prompt del LLM — Task 4
- ✅ Telegram bot comandos directos (12 comandos) — Task 5
- ✅ Telegram bot comandos LLM (/analizar, /proponer, texto libre) — Task 5
- ✅ Owner filter (solo responde al chat_id configurado) — Task 5
- ✅ Bot en thread separado en run.py — Task 5
- ✅ Tests de todo — Task 6

**Consistencia de tipos:**
- SystemController.pause/resume llaman scheduler.pause_job/resume_job ✅
- _api() retorna dict siempre ✅
- classify_multitimeframe recibe 3 strings literales ✅
- get_closed_trades retorna list[Trade] ✅
- start_bot arranca asyncio loop propio ✅
